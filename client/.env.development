# Dev-mode override: `npm run dev` serves the UI from Vite, which proxies
# /api, /posters and /uploads to localhost:5000 (see vite.config.ts). Keeping
# this empty leaves those requests relative so the proxy handles them, which
# means desktop dev keeps working even when the LAN IP in .env goes stale.
VITE_API_BASE_URL=
